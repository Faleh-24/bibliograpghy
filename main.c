#include <stdio.h>
#include <string.h>
#include "bibliography.h"

int main(void) {


    int runnung=1;
    int count = readBiblioGraphyData();
    while (runnung ==1)
    {

        getUserMenu();

    }

    return 0;
}
